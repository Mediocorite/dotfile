alias vim=lvim
alias vi=vim

# Bind the function to Ctrl+F
#bind \cf tmux_fzf

#function lvim
 #   if not test -n "$TMUX"
  #      tmux new-session -A -s lvim "lvim $argv"
   # else
    #    command lvim $argv
    #end
#end

